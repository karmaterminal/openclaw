# Update notes for #1257

URL: https://github.com/karmaterminal/openclaw/issues/1257
Title: Multi-gigabyte agent SQLite transcript reads synchronously block gateway turns
Project 87: Todo
Parent: #1254
#121204: independent
#1256: same-trace amplifier, **different owner**. Do not wait; do not combine.

## Keep

DB sizes and representative holds:

| Seat   |        Agent DB | Hold                          | Same-trace companions              |
| ------ | --------------: | ----------------------------- | ---------------------------------- |
| Cael   | 5,403,803,648 B | 4,864ms / 5,398ms fenced read | context-unavailable + zero payload |
| Ronan  | 3,796,570,112 B | 2,327ms / 2,543ms             | finalizer fail + zero payload      |
| Emeric | 6,622,703,616 B | 1,222ms `agent.write`         | finalizer 76ms later               |

Owner: `loadTranscriptEventsSync` → sync deferred txn + JSON parse of **selected** session/fence rows (`session-accessor.sqlite-read.ts:47-60,152-168`). Not the shared ingress state DB.

Holds are latency/liveness amplifiers. They are not proved to cause context rejection.

## Corrections

1. Causal prose “reads all transcript event_json” is too broad. Production selects `session_id` and optional `seq < fence`. On multi-GB stores that set can still be huge. Keep the filed “selected” wording.

2. #119901 / #119884 are **candidates**. Planner stats were not read. Do not treat offline `ANALYZE` as the fix until a second immutable copy proves stale `sqlite_stat1` and a timing delta.

3. Cael quick-check corruption lives only in the #1254 comment. Path/freelist receipts are missing. Keep integrity/compaction as a **separate** lane.

4. Current upstream `fc4d5d744fa` (#123495) protects readable transcripts from cleanup. Observed holds successfully read for seconds; that commit does not apply.

5. `ed447135bd4` (shared WAL) does not reach agent-DB holds.

## Minimal owner-correct intervention

Decide from copied-store evidence, in this order:

1. If stale planner stats: rebase/supersede #119901 as offline doctor `ANALYZE` after compaction.
2. If the query/fence itself is the stall: change the **read owner** (streaming, narrower fence, or move parse off the txn) without weakening fencing/order.
3. If physical corruption: recovery lane, not this PR.

No runtime retries, no larger timeouts, no live VACUUM/ANALYZE.

## Fossil / controls (missing)

On a verified immutable copy **after Gateway release** (or filesystem snapshot):

- `PRAGMA integrity_check` / `quick_check` (copy only)
- `sqlite_stat1` presence, page/freelist counts
- `EXPLAIN QUERY PLAN` for the exact fenced read
- wall/CPU for `loadTranscriptEventsSync` before/after `ANALYZE` on a **second** copy
- transcript byte/seq equality control
- unrelated small-session control

Incident-shaped copied-store proof required before fleet deploy.

## Non-goals

No live maintenance, no speculative corruption repair, no ingress-queue conflation, no weakening fencing, no continuation.

## Status recommendation

Todo. May start in parallel with #1256 **only** on copies. `Done` only after proof publication + disposition.
