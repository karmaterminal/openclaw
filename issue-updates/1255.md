# Update notes for #1255

URL: https://github.com/karmaterminal/openclaw/issues/1255
Title: Ingress abandonment bypasses retry budget and can loop indefinitely
Project 87: `in_coding_agent` (keep)
Parent: #1254
#121204: independent (shared `ingress-drain.ts` file only)

## Keep

Symptom, Discord cadence (42 / ~1h46m / ~180s cap), owner path, cancellation budget-free, next-row progress, no live Silas mutation, independence from #121204.

Discord policy cited by the fossil is correct on `6b09`:

```ts
retryPolicy: { maxAttempts: DEFAULT_INGRESS_RETRY_MAX_ATTEMPTS, deadLetterMinAgeMs: 0 }
```

`extensions/discord/src/monitor/ingress.ts:552-555`.

## Corrections

1. Replace “strongest proven defect” with:
   - **Observed:** same source event retried 42 times; cadence caps at `DEFAULT_INGRESS_RETRY_MAX_MS`.
   - **Source-localized:** `onAbandoned` → `releaseUnadopted` (`ingress-drain.ts:504-506`) **BYPASSES** `resolveIngressFailureDisposition` (`ingress-retry-policy.ts:77-120`).
   - **Not yet:** published RED receipt, GREEN/revert closure, or isolated live Gateway proof.

2. Remove from this issue’s desired contract:

   > The exact session-lifecycle rejection reason is preserved rather than collapsed.
   > That is `defect:silas-rejection-reason` (`reply-turn-admission.ts:263-325`), a separate follow-up. Upstream #118873/#118879 do not close the abandonment budget.

3. Fossil status: branch `codeagent/silas-abandonment-red-fossil` @ `6f4ef385ea7881b3572efef9e06907d23c24fe90` adds `src/channels/message/ingress-drain.abandonment-retry-budget.test.ts`. **No published RED log.** Do not mark the characterization gate closed until the sanctioned runner receipt is attached.

4. #1254’s first comment (14 attempts / 17-deep backlog) is an earlier snapshot. Prefer the later 42 / 1h46m journal window.

## Minimal owner-correct intervention

Route pre-adoption `onAbandoned` through the same bounded disposition as `onFailed`, with `lastError` remaining `turn-abandoned` (or a dedicated abandonment reason that still hits `retry-limit-exceeded`). Keep `onCancelled` / shutdown on `recordAttempt: false`. Do not change FIFO freshness from #121204.

## Fossil / controls (already authored; need RED receipt)

Desired-contract RED (must fail on `6b09` / fossil branch before the patch):

- eight `onAbandoned` passes with incident retry policy
- terminal `retry-limit-exceeded`, payload retained, `message` still abandonment identity
- follower row in the same lane then adopts

Negative controls (must stay GREEN before and after):

- below-ceiling abandonment remains pending
- cancellation at the ceiling is budget-free
- unrelated lane still completes
- ordinary thrown dispatch still dead-letters on the existing `onFailed` path with a **distinct** message

Walk: `6b09`, absorbed `530b`, current upstream. Then GREEN → patch-only revert RED → reapply GREEN.

Live/incident-shaped: isolated Gateway + state dir, no Discord send, only after the code fossil closes. Do not mutate Silas.

## Non-goals

No live queue edits, no Silas DB/session mutation, no #121204 expansion, no session-owner repair, no continuation, no schema bump.

## Status recommendation

Keep `in_coding_agent` until the RED receipt is published to `PR-NNNNNN/PROOFS/<SHA>/`, then remain there through the budget patch and closure. `Done` only after proof publication + deployment disposition.
