# Update notes for #1259

URL: https://github.com/karmaterminal/openclaw/issues/1259
Title: Prepared provider route materialization needs typed failure reasons before fallback
Project 87: Todo
Parent: #1254
#121204: independent
#1256/#1257: independent families

## Keep

Elliott fallback order (passive window): Terra materialize fail → Sol materialize fail → GitHub Copilot candidate → visible HTTP 401. 72 OpenAI candidate-failure rows; two #1254 401 receipts.

Owner: `materializePreparedRuntimeModel` (`src/agents/runtime-plan/materialize-model.ts:99-135`) collapses:

- missing resolved model
- provider mismatch
- model ID mismatch
- route API / base URL / auth-requirement mismatch

into `resolved.error` or one generic `Unable to materialize …` string.

Desired contract: typed redacted reason recorded on the existing fallback decision. Do not weaken route exactness. Expired Copilot cleanup/config is a **separate** issue.

## Corrections

1. Offline `openclaw models status --probe` ≠ live prepared-route/fallback chain. Do not use a healthy CLI probe as Elliott’s OpenAI proof.

2. Visible 401 is not proved to be the prepared OpenAI route. Copilot is the later candidate.

3. #114603 applies only if token/SecretRef + `codexProxyBaseUrl` are independently established. Elliott config was not read.

4. Current upstream after `530b` does not change this function (re-checked `git log 530b..upstream/main -- materialize-model.ts`).

5. No fossil yet. Reason enum + negative controls are the first PR.

## Minimal owner-correct intervention

Return a closed reason (`missing-model` | `provider-mismatch` | `model-id-mismatch` | `route-mismatch`) plus normalized API/base-URL **class** and auth requirement. No secrets, no host/path that is credential-bearing. Record it on the existing fallback decision row.

Repair only the selected owner after one isolated, delivery-disabled candidate run.

## Fossil / controls (missing)

- one RED per predicate with real-shaped prepared routes
- negative: exact match still materializes
- redaction: no token/url secret in logs or fallback rows
- Copilot 401 path is a **control**, not this fossil
- no fallback reorder in the reason-only PR

Authenticated provider probe is an external side effect: explicit operator approval, one candidate, isolated session, delivery disabled.

## Non-goals

No fallback reorder, no reauthentication, no secret output, no weakening equality, no continuation, no live Elliott config write.

## Status recommendation

Todo. Last of the first wave. `Done` only after proof publication + disposition. Do not deploy a “fix” that only deletes the Copilot fallback.
