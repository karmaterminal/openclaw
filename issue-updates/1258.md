# Update notes for #1258

URL: https://github.com/karmaterminal/openclaw/issues/1258
Title: Ingress completion clears terminal outcome lineage needed to diagnose silent replies
Project 87: Todo
Parent: #1254
#121204: independent. 121204 may already complete stale-ambient as a handled failure (`stale-ambient-backlog`); that is one future enum value, not this issue’s owner.

## Keep

Rune: ingress `completed`, no visible reply, no retained diagnostic. `completed` means transport replay custody ended. Policy-gate / no-dispatch currently calls `onAdopted()` (`ingress-monitor.ts:446-450`). `complete()` wipes `payload_json` / `metadata_json` (`ingress-queue.ts:1051-1121`).

Desired contract: bounded, payload-free lineage (outcome enum + source event ID + run ID when known). One admitted event → one terminal outcome. No message content. No identity inference. No Rune delivery invention until the enum selects an owner.

## Corrections

1. **Do not add a column.** `completed_metadata_json` already exists and is nullable. Populate it. No schema-version bump, no dual-write, no companion table.

2. “Causal report proves missing discriminator is the blocker” is slightly strong. Source proves completion is not delivery and that the discriminator is absent. Rune’s actual branch (gate vs agent vs delivery vs intentional silence) is **unknown**.

3. #114137 / #121058 show zero payload can be intentional `NO_REPLY`. Rune has no zero-payload warning, so those closes do not classify Rune.

4. Fork #1229/#1188/#787 do not supply this lineage.

## Minimal owner-correct intervention

Define a closed payload-free completed-metadata enum, for example:

- `policy-gate`
- `adopted-agent-run`
- `direct-visible-delivery`
- `intentional-silence`
- `terminal-error`
- `stale-ambient-suppressed` (121204-class; write it if that path completes)

Write it at the producer that already calls `complete()` / `onAdopted()`. Discord preflight supplies transport-specific values; core owns the generic enum.

## Fossil / controls (missing)

- every enum value written and read back
- payload/content redaction: completed row has no message body
- policy-gate vs adopted-agent-run are distinguishable
- one isolated nonce: 1 event → 1 run or explicit gate → 1 terminal → visible snowflake **or** recorded non-delivery
- unrelated channel/lane control

Live Discord nonce requires operator approval + isolated test channel.

## Non-goals

No message retention, no identity inference, no broad telemetry redesign, no live probe without approval, no Rune speech “fix,” no schema bump, no continuation.

## Status recommendation

Todo. Start after #1255 is claimed if a seat is free. Required before any Rune treatment. Never `swim`.
