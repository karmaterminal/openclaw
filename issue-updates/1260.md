# Update notes for #1260

URL: https://github.com/karmaterminal/openclaw/issues/1260
Title: Reply-turn admission collapses three lifecycle rejection reasons into one error
Project 87: Todo
Parent: #1254
Ward serialization: after #1257, before #1259 (`#1255 → #1256 → #1258 → #1257 → #1260 → #1259`)
#121204: independent
#1255: independent owner. Budget repair must not guess this reason.

## Keep

This is the previously named follow-up `session-admission-reason-code`. Filing it as its own issue is the correct cut.

Three `admitReplyTurn` producers share one message (`src/auto-reply/reply/reply-turn-admission.ts:263-325` on `6b09`):

1. expected session ID mismatch
2. `claimMainSessionRecoveryOwner()` returns `invalidated`
3. interruption/abort before operation

Filed names: `expected-session-mismatch` | `recovery-owner-invalidated` | `pre-operation-interrupted`.

Desired first PR is **instrumentation only**: attach a closed typed reason to the existing lifecycle-invalidated error. Preserve exact throw, user-visible text, and queue behavior. No admission/retry/recovery repair until an isolated/session-copy receipt selects Silas’s branch.

If that branch is `recovery-owner-invalidated`, evaluate upstream #118879 instead of duplicating it. #118873/#118879 own terminal restart-recovery residue, not the collapsed diagnostic itself.

## Corrections

1. This is a missing discriminator, not a proved Silas root cause. Causal graph: `defect:silas-rejection-reason` CHARACTERIZES the loop’s producer only after a reason is retained. The 42-attempt loop is still owned by #1255.

2. “Base is RED because all branches are indistinguishable” is the right fossil shape. There is **no fossil yet**.

3. Do not change user-facing text in the instrumentation PR. A reason field / structured code is the contract, not a new string that would break #1115-style classifiers or operator grep.

4. Mutation gate is correct: temp session/store only. No live Silas session reset, recovery-owner claim, ingress-row edit, Gateway restart, or retry-policy change.

5. Label is `non-continuation` only. Recommend also `bug` to match #1254.

## Minimal owner-correct intervention

Add a closed reason on the existing invalidated error object (or a sibling typed field that survives into durable `lastError` / diagnostic metadata without replacing the visible message). Wire all three call sites. Do not alter skip/owned/active-run deferral.

Behavioral successor is a **later** issue after `R-NC-SILAS-REASON`.

## Fossil / controls (missing)

Filed rows, kept:

- `R-NC-ADMISSION-REASONS` — all three branches distinguishable
- `R-NC-ADMISSION-MESSAGE` — visible text unchanged
- `R-NC-ADMISSION-SUCCESS` — successful admission + active-run deferral unchanged
- `R-NC-SILAS-REASON` — isolated/session-copy receipt; pending

Plus GREEN → patch-only revert RED → reapply GREEN on the instrumentation patch.

## Non-goals

No #1255 budget patch, no #118879 rewrite, no live fleet session surgery, no continuation, no user-text rewrite, no retry-policy change.

## Status recommendation

Stay **Todo** until the serialized predecessor #1257 is claimed or a spare worker exists after #1255/#1256/#1258. Never `swim`. `Done` only after proof publication + disposition. Do not open a behavioral successor from this issue.
