# Update notes for #1256

URL: https://github.com/karmaterminal/openclaw/issues/1256
Title: Codex settled-turn finalization erases rejection reason and yields zero visible payload
Project 87: Todo (keep until a coding agent is assigned)
Parent: #1254
#121204: independent
#124176 / #122076: yield-specific; **do not merge**. Fleet traces had no yield marker.

## Keep

Same-trace ordered receipts:

| Seat   | Trace                              | Failure      | Zero-payload warning |
| ------ | ---------------------------------- | ------------ | -------------------- |
| Cael   | `818314b20865025cc092d3042672f63b` | 09:27:21.779 | 09:27:27.938         |
| Ronan  | `39bb0342c1c8abf08d305343b75028a6` | 09:55:12.726 | 09:55:24.971         |
| Emeric | `9af101e0eeb3b3058bc3810e5b8851ad` | 09:38:03.172 | 09:38:06.507         |

Owners on `6b09`:

- `extensions/codex/src/app-server/settled-turn-context.ts:101-180` (and capture `:192-194`)
- `extensions/codex/src/app-server/run-attempt-finalize.ts` (throws “context is unavailable”)
- catch keeps empty prep: `src/agents/embedded-agent-runner/run/settled-turn-finalization.ts:156-166`
- observer only: `src/channels/turn/execution.ts:85-118`

Do not weaken transcript attestation. Do not amalgamate with #1257.

## Corrections

1. Title/body should say **same-trace sequence**, not that reason erasure is the proved payload killer. Inner `undefined` exit is unknown. Capture can also fail on missing history.

2. Reason enum must include a capture-level `missing-history` in addition to the six builder exits listed. Filed list omitted it.

3. There is **no fossil yet**. “Replay an incident-shaped fossil” is the next probe, not current evidence.

4. `messageId=unknown` on finalized paths still blocks joining the original Discord event `1538222293149294694` to the failed run. That join is a #1258/#1256 shared observability debt; do not fake it.

5. Codex contract (`@openai/codex@0.147.0`, `be6e8eac…`): `turn/completed` is last-agent-message summary, not a full transcript. OpenClaw owns the mirror. Cite Codex files in any repair PR; this review re-checked the causal report’s citations only (no sibling `../codex` checkout in this worktree). Repair agents must inspect sibling Codex before a Codex verdict.

## Minimal owner-correct intervention

Phase A (this issue’s first PR): return a closed redacted reason; bind it to existing trace/run/turn IDs; do not change attestation predicates.

Phase B (only after one reason is RED on a copied agent DB): repair that owner branch only.

A mocked executor that returns `undefined` is not incident causality.

## Fossil / controls (missing)

RED on an immutable copied agent DB, no provider creds, no channel send:

- reason code is present and stable
- unrelated session control stays unused
- yield-detected path is a **negative control** (must not be the fleet fossil)

Then GREEN → revert RED → reapply GREEN on the reason-only or owner-fix patch as scoped.

Incident-shaped executor/transport receipt required before claiming Cael/Ronan/Emeric speech cured.

## Non-goals

No live DB replay, no weakening exactness, no generic zero-payload suppression, no SQLite optimization, no #124176 fold, no continuation.

## Status recommendation

Stay **Todo** until assigned. Do not start before #1255 is claimed; may run in parallel after that. `prince_review` only if a prince is actually reviewing. Never `swim`.
