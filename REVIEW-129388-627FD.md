# Independent review: covenant compaction semantic successor

Bound item: [openclaw/openclaw#129388](https://github.com/openclaw/openclaw/pull/129388)

## Verdict

`REQUEST_CHANGES`

The two-file semantic repair is correct. It restores the exact semantic-merge test
blobs, exercises the current compaction harness and failure-resolution APIs, adds no
production delta, and preserves the schema-v19 parent. The exact successor commit is
not acceptance-ready because its body contains literal `\n` bytes: Git parses no
`Co-authored-by` trailer. Recreate the commit with the same tree and a real multiline
body/trailer, then re-anchor the successor and review-lane refs to that new commit.

The inherited test-type and graph-cap failures are not caused by this successor. They
are identical on its direct parent and absent on pinned upstream; they remain
fork-integration debt rather than a reason to alter these two test blobs.

## Named-ref contract

This table records the evidence cut before the report-only commit. The safe review
branch was published unchanged before any lane evidence was credited.

| Category         | Named ref                                                              | Full SHA                                   | Local/tracking/server equality                                                     |
| ---------------- | ---------------------------------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------- |
| Product/base ref | `origin/codeagent/129388-compact-hook-semantic-contract-cure-20260830` | `627fd528391d62add9c828b3896e8864adb6de22` | local product object, remote-tracking ref, and server branch equal                 |
| Safe review ref  | `codeagent/129388-627fd-semantic-contract-independent-review-20260830` | `627fd528391d62add9c828b3896e8864adb6de22` | local branch, remote-tracking ref, and server branch equal; no configured upstream |
| CI/workflow ref  | N/A                                                                    | N/A                                        | focused-only review; no Mode-B workflow dispatched                                 |
| Presentation ref | `origin/savegame/129388-covenant-final-00c7f721-20260828T1203Z`        | `00c7f721a55554d0b9228337cc8bc6bec88f9e9f` | local remote-tracking ref and server branch equal; protected and out of scope      |
| Docs/proof ref   | N/A                                                                    | N/A                                        | covenant proof harness, corpus, docs, and fleet were out of scope                  |

Supporting immutable commits:

| Role                            | SHA                                        | Identity result                                                                             |
| ------------------------------- | ------------------------------------------ | ------------------------------------------------------------------------------------------- |
| Gate-2-green/runtime-red parent | `e6b9dcbd1f365ba02d994a8ee24e2c3fd89af900` | local commit object; successor's sole parent                                                |
| Gate-2 accepted-blob commit     | `c842f07c267d98ca2c3edb6c29eedfaf2be32a52` | local commit object; direct parent of `e6b9dcbd`                                            |
| Semantic merge reference        | `3b854dd1862cae7208f3e031a0a08d276cd8626e` | local commit object; parents `9de8dd7fcfb34fa1769ff0bfac3790818dc44a15` and pinned upstream |
| Pinned upstream                 | `43a7cb3c92c7b5b8d5ddd56d9d157c009e0c85e5` | local commit object; exact second parent of the semantic merge                              |
| Current Gate-2 tooling source   | `karmaterminal/openclaw-bootstrap` `main`  | `e768ccc2e1e0887be455e6880db0bff91a1dfddd`; local `origin/main` and server equal            |

## Commit, tree, and worktree identity

- Successor: `627fd528391d62add9c828b3896e8864adb6de22`
- Sole parent: `e6b9dcbd1f365ba02d994a8ee24e2c3fd89af900`
- Successor tree: `52b6141c80e575813f94241635ce02007b50d140`
- Parent tree: `bfd2f9100d1afc30146a772e22eeed606122ac7f`
- A temporary index seeded from `e6b9dcbd` and updated only with semantic-reference
  blobs `aab495224dc99cd52de60a7f599a5eaf9a7f83b4` and
  `b30739d70905a77cf58a044341a0b46e9d5867a4` writes tree
  `52b6141c80e575813f94241635ce02007b50d140`, exactly the successor tree.
- `git diff --name-status e6b9dcbd..627fd528` reports only:
  - `src/agents/embedded-agent-runner/compact.hooks.test.ts`
  - `src/agents/embedded-agent-runner/compact-reasons.test.ts`
- LOC classification: production `+0/-0`; tests `+457/-103` (net `+354`).
- The schema-v19 parent blobs are retained exactly:
  - `src/state/openclaw-agent-db-schema.ts`:
    `47ae35587872d0040cb8d61e1c94d41444b0ef9a`
  - `tmp-drop-me-claude.md`:
    `134864bd5ad92665fadc63f3269bf1ae3ec9990c`
- The worktree was not literally clean at review start: it contained a pre-existing
  `pnpm-lock.yaml` modification plus untracked `BRIEF.md` and `launch.sh`. The lockfile
  returned to its committed byte during tool setup; the two untracked dispatch files
  remained untouched. Before report authoring, both changed test working-tree blobs
  matched `HEAD`, all tracked product files were clean, and the exact-SHA dependency
  clone was clean.

### Commit trailer defect

`git cat-file commit 627fd528...` shows one body line containing:

```text
... APIs.\n\nRefs openclaw/openclaw#129388.\n\nCo-authored-by: Copilot <223556219+Copilot@users.noreply.github.com>
```

Those are literal backslash-plus-`n` bytes, not newlines.
`git log -1 --format='%(trailers:key=Co-authored-by,valueonly)' 627fd528...`
returns empty output. The visible text therefore does not satisfy the required commit
trailer contract.

## Two-file semantic assessment

| File                      | Gate-2 accepted blob (`c842f07` / `e6b9dcb`) | Semantic merge (`3b854dd`)                 | Successor (`627fd52`)                      | Pinned upstream (`43a7cb3`)                |
| ------------------------- | -------------------------------------------- | ------------------------------------------ | ------------------------------------------ | ------------------------------------------ |
| `compact.hooks.test.ts`   | `63ec1378c30fa6c86fba102e75a5552c79b4807d`   | `aab495224dc99cd52de60a7f599a5eaf9a7f83b4` | `aab495224dc99cd52de60a7f599a5eaf9a7f83b4` | `9c2bca6b948c98165ced3b4d5baab3113a923541` |
| `compact-reasons.test.ts` | `475a9513d70d507979a7febe97ebb062523f376a`   | `b30739d70905a77cf58a044341a0b46e9d5867a4` | `b30739d70905a77cf58a044341a0b46e9d5867a4` | `dcfd7b257f89e95d0a5953eb43b9f7d39eda4265` |

The successor is byte-identical to the semantic merge for both files. It is not
byte-identical to the old Gate-2 blobs, and that difference is required rather than a
regression.

### `compact.hooks.test.ts`

The current test harness owns a required workspace fixture:

- `compact.hooks.harness.ts:596` declares
  `resetCompactHooksHarnessMocks(workspaceDir: string)`.
- `compact.hooks.harness.ts:611` immediately calls
  `join(workspaceDir, "agents/main/agent")`.
- The same value feeds `fixtureWorkspaceDir`, `resolveAgentDir`, and
  `resolveAgentWorkspaceDir`.

The Gate-2 blob calls the reset with no argument. The semantic/successor blob creates a
per-test workspace, derives its session file from that workspace, closes the real
agent database before cleanup, and passes the workspace to the reset. Its path changes
also remove shared fixed `/tmp` SQLite identities and canonicalize real paths where
the production resolver does so.

The successor retains upstream's current compaction coverage and adds covenant
composition checks for routed-peer account history, nested continuation-tool
suppression, final session permission policy, runtime-provider selection, and
safeguard fallback provenance. The test inventory grows from 154 parent tests to 165;
it is not weakened.

### `compact-reasons.test.ts`

The production owner is now `resolveCompactionFailure`, not the retired
`resolveCompactionFailureReason`:

- `compact-reasons.ts:76` accepts the thrown `error`, optional structured safeguard
  cancellation, and optional caller abort signal, then returns both display `reason`
  and causal `error`.
- `compaction-session-execution.ts:599` uses that shape while choosing a
  reasoning-level retry.
- `compaction-session-execution.ts:643` consumes the same structured cancellation
  before returning the terminal failure.

The Gate-2 blob invokes a nonexistent function and tests only display strings. The
semantic/successor blob tests the current structured contract: generic wrapper
recovery, provider status/code retention, intentional quality-decline isolation,
genuine error precedence, and caller-abort precedence. It also retains the covenant
closed reason/skip classification. The test inventory grows from 57 parent tests to
64; it is not weakened.

One nonblocking cleanup opportunity remains in the inherited semantic blob:
`ALL_CODES` omits `auth_failed` even though the production union includes it. Existing
authentication classification is independently covered, so this is not caused by or
blocking for the successor, but a later table-driven exhaustiveness cleanup should
make the list structurally exhaustive.

## Negative and positive controls

All focused tests ran on AMD64 (`x86_64`) with Node `v24.20.0`, pnpm `12.0.0`,
Vitest `4.1.11`, one worker, and repository-owned runners/configs.

### Parent negatives

At exact parent `e6b9dcbd1f365ba02d994a8ee24e2c3fd89af900`:

```text
node scripts/run-vitest.mjs run \
  --config test/vitest/vitest.agents-embedded-agent.config.ts \
  --maxWorkers=1 src/agents/embedded-agent-runner/compact.hooks.test.ts \
  -t 'returns a summaryless xAI manual endpoint result'
```

Result: one selected test failed. `resetCompactHooksHarnessMocks()` reached
`join(undefined, "agents/main/agent")` at `compact.hooks.harness.ts:611` and emitted:

```text
TypeError: The "path" argument must be of type string. Received undefined
Serialized Error: { code: 'ERR_INVALID_ARG_TYPE' }
```

The correctly routed parent reason suite also fails deterministically:

```text
node scripts/run-vitest.mjs run \
  --config test/vitest/vitest.unit-fast.config.ts \
  --maxWorkers=1 src/agents/embedded-agent-runner/compact-reasons.test.ts
```

Result: 2 failed, 55 passed; both failures are
`TypeError: resolveCompactionFailureReason is not a function`.

### Successor positives and siblings

| Owner route                              | Focus                                                                                              | Result                         |
| ---------------------------------------- | -------------------------------------------------------------------------------------------------- | ------------------------------ |
| `vitest.agents-embedded-agent.config.ts` | hooks plus abort, native CLI, server endpoint, runtime-context, and post-compaction-loop siblings  | 6 files, 242 passed            |
| `vitest.unit-fast.config.ts`             | correctly routed compact reasons, duplicate-user-message sibling, and failover-error owner         | 3 files, 148 passed            |
| `vitest.unit-src.config.ts`              | schema-v19 participant convergence and agent DB owner                                              | 2 files, 137 passed, 6 skipped |
| `vitest.runtime-config.config.ts`        | recipient authority rollover/fallback/compaction/reopen owner                                      | 1 file, 7 passed               |
| `vitest.gateway-server.config.ts`        | restart sentinel and `/new`/`/reset` recipient authority                                           | 2 files, 86 passed             |
| `vitest.auto-reply-reply.config.ts`      | preparation/final-adoption fences, system events, media-only admission, terminal-notice durability | 4 files, 176 passed            |

This covers success, intentional decline, provider failure, caller abort, persistence,
rollback, restart/reopen, partial failure, session replacement, restrictive
visibility, and the nearest native/server-endpoint alternatives.

## Focused static checks and baseline-red classification

The pinned Knip tool was resolved from fresh lane-local stable cache/store paths.
`pnpm dlx --package knip@6.32.2 knip --version` returned `6.32.2`.
`scripts/check-deadcode-exports.mts` parsed all three scans:

- script unused exports: 0
- full-tree unused exports: 0
- production unused exports: 0

Direct changed-path checks passed:

- `oxfmt --check` on both files
- focused core oxlint on both files
- `check:max-lines-ratchet --base e6b9dcbd...`
- `git diff --check e6b9dcbd..627fd528`

`node scripts/check-changed.mjs -- <two test paths>` passed conflict-marker,
max-lines, assertion-safety, attribution, doctor-registry, wildcard re-export,
duplicate-target, coercion, dependency-pin, format, deprecated-API, plugin-boundary,
wrapper-shadowing, patch, and exact dead-export checks. It then stopped on the known
core-test graph boundary.

| Diagnostic                    | Successor                                                                        | Parent `e6b9dcbd`                 | Pinned upstream `43a7cb3c` | Attribution                                                                                           |
| ----------------------------- | -------------------------------------------------------------------------------- | --------------------------------- | -------------------------- | ----------------------------------------------------------------------------------------------------- |
| `lint:tmp:tsgo-core-boundary` | fail: `agents-root` 722 and `agents-other` 723 exceed 720                        | identical fail                    | pass                       | fork graph-cap debt already present on parent; unchanged test-file count means successor adds no root |
| `tsgo:core:test`              | fail: 21 diagnostics across 9 unrelated continuation/system-prompt test fixtures | exact same 21-line diagnostic set | pass                       | fork fixture drift already present on parent; neither changed compaction file appears                 |

The type diagnostics are missing required `SpawnSubagentResult.context` in eight
subagent/continuation fixture files and stale `task` fields in
`src/agents/system-prompt.test.ts`. They are real fork-integration failures, not
upstream failures and not successor regressions.

The changed-path temp-creation report is warning-only. It emitted 28 diagnostics:
26 are unrelated continuation/plugin/core fixture debt. The two successor lines in
`compact.hooks.test.ts` are intentionally bounded: the shared default store closes in
`afterAll`, and the legacy marker store closes in `finally`. Neither leaked in the
serial owner run.

No Mode-B workflow was dispatched. This review's acceptance path is
`focused-only`; the inherited red static gates are disclosed rather than represented
as broad acceptance.

## Gate-2 primitive inventory mismatch

Current bootstrap `main` at
`e768ccc2e1e0887be455e6880db0bff91a1dfddd` still lists both shared upstream-owned
test files as whole-file primitive cores in
`tools/drift-cure-gate.primitive-cores.txt:56,59`.

`tools/feature-cores-byte-check.sh` can express:

1. an unchanged first-parent blob;
2. a tombstone; or
3. the exact projection of an upstream delta onto the first parent.

It cannot express a reviewed semantic merge that combines current upstream test
ownership with covenant-specific assertions. The semantic merge therefore failed the
byte gate, and `c842f07` restored first-parent "accepted" blobs that were already
incompatible with the merged harness and failure API. The gate was mechanically
consistent with its inventory but the inventory was semantically wrong; its green
result did not establish runnable test ownership.

The smallest bootstrap-side cure is to remove these two mutable shared test files from
the primitive byte inventory while retaining the production owners
(`compact-reasons.ts`, `compact.hooks.harness.ts`, `compact.ts`, and
`compact.types.ts`). Route the two test files through the existing semantic-conflict
owner-test gate instead. If continuation needs exact-byte protection, rehome only its
dedicated assertions to continuation-owned test files and inventory those files; do
not add another accepted-blob exception for the shared upstream suites.

No bootstrap file was edited.

## GitNexus receipt

- Installed launcher: `/home/figs/.local/bin/gitnexus`
- CLI/package version: `1.6.5`
- Launcher SHA-256:
  `a55ef1efafd62ead5bdf9208cb96b410378e953b84390d2b0cd1d139c5953e7e`
- Installed fork source: `/home/figs/src/gitnexus`
- Remote: `https://github.com/karmaterminal/GitNexus.git`
- Fork SHA: `3c1e686edfc1acaac882927cada121ddd7c47bcc`
- Built CLI SHA-256:
  `00f67e34c0ef3a7ea5f1665247699f47e7e2eab2dc233a504fe95d9aa11d8590`
- Exact successor index SHA/path: N/A. `gitnexus status` reported this checkout
  unindexed, and the global registry contained no
  `627fd528391d62add9c828b3896e8864adb6de22` index.
- Query shape: N/A; no stale graph was substituted.

The explicit fallback was direct blob hashing, synthesized-tree comparison, full diff
review, call-site search, history/blame, owner/sibling source reading, and focused
owner execution.

## Simplification and residual uncertainty

The product-side repair is already the simplest safe shape: restore two exact reviewed
test blobs and touch no production owner. Re-hand-merging or partially copying these
tests would add divergence and recreate the Gate-2 failure mode. No product
simplification is recommended in this successor.

Residual uncertainty:

- The initial worktree was not literally clean, although tracked product/test bytes
  were exact before evidence runs and the remaining untracked dispatch files were not
  imported or staged.
- No exact successor GitNexus index existed.
- No Mode-B broad acceptance was run.
- The fork's inherited graph-cap and continuation fixture type failures remain red and
  require a separate owner lane.

The requested successor change is metadata-only: preserve tree
`52b6141c80e575813f94241635ce02007b50d140`, recreate the commit with real body
newlines and a parsed Copilot co-author trailer, then re-run the identity gate.

`READY_FOR_SCRIBE_REVIEW`
