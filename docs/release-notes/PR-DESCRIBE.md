# PR-DESCRIBE variants

**Variant A: full-canonical2** (old `feature/context-pressure-squashed` base `90db3699ccf3` -> canonical2 tip `cf7830ffb370`: 1013 files, +65462/-8970). Canonical2 moves continuation onto a TaskFlow-backed delegate substrate, preserves one-cycle legacy `taskFlowDelegates` config tolerance, and adds durability proof for chain state across ordinary runner, followup runner, child-drain, and restart paths. The audit lane fixed durable write-back after tool-delegate dispatch (PR #427, `d0f31f65cc1250e5300d1c45ac4feeda71100b18`), followup-token advancement when `dispatched === 0` (PR #428, `e73fd0f088813ca125bab60a2cc54c08ac97ff07`), child-drain persistence (PR #429, `dc572c01062a8da9a337039c87c1eb09288af640`), and the #431 disk orphan (PR #432, `cf7830ffb3702bf7d826d70838893e2e41709f12`). It also carries swim-37 durability proof and broader continuation telemetry/context-pressure work; #368, #361, and #363 remain pre-squash decisions.

**Variant B: audit-lane-narrow** (old `feature/context-pressure-squashed` base `90db3699ccf3` -> current `origin/feature/context-pressure-squashed` `63d5c8c65ea3`: 33 files, +5267/-2; path-filtered: 30 files, +5147/-2). This narrower round ports the continuation durability hardening needed for the audit lane: the ordinary runner durable write-back, followup `dispatched === 0` token persistence, child-drain chain-state persistence, the swim-37 S1/S2/S3 durability harness, and the #431 followup `updateSessionStore` fix. It intentionally leaves broader canonical2 surfaces for follow-up review, including #368 TaskFlow-only purge, #361 RFC/OTEL iteration, #363 multi-recipient descriptors, and other non-audit-lane canonical2 churn.
