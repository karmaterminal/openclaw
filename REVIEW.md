# Phase 3 critical review: v5.2 recompose squash branch

## Verdict

**REQUEST_CHANGES** before Phase 5 force-push or upstream PR presentation.

I reviewed `feature/context-pressure-squashed-recompose-20260504-findings-1-2-3` at `402c44604411b2ab5910df330864d7ff833d2cc4` against upstream tag commit `8b2a6e57fef6c582ec6d27b85150616f9e3a7ba4`. The diff inventory is exactly 351 files and roughly 40,495 insertions / 904 deletions. The load-bearing Finding 1/2/3 code anchors are present, but the branch is not upstream-presentable: it still carries fork/cohort artifacts, internal trackers, branch-lane notes, and a `studies/swim-37` scaffold wired into default repo test/typecheck surfaces.

Follow-up issues filed:

- https://github.com/karmaterminal/openclaw/issues/592
- https://github.com/karmaterminal/openclaw/issues/593

## Walk coverage

All 351 changed files were walked for scope, upstream-leakable content, and surprise regressions. File-surface inventory:

| Surface                          | Files |
| -------------------------------- | ----: |
| `src/agents/**`                  |   114 |
| other root/core files            |    71 |
| `src/auto-reply/reply/**`        |    49 |
| `src/infra/**`                   |    31 |
| `src/auto-reply/continuation/**` |    25 |
| `studies/**`                     |    21 |
| `extensions/**`                  |    16 |
| `docs/**`                        |     8 |
| `test/**` config                 |     6 |
| `apps/**`                        |     5 |
| `scripts/**`                     |     3 |
| `.github/**`                     |     1 |
| `packages/**`                    |     1 |

## Scope concerns

| Finding                                                                                                                          | Evidence                                                                                                                                                                                                                                             | Recommendation                                                                                                  |
| -------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------- |
| Branch-specific planning docs are in the upstream diff.                                                                          | `docs/plan/surfaces-1-2-substrate-notes.md:1-15` records lane branch, descriptor source, cohort ratification, and local/fork refs; `docs/pr-bodies/pr-38780-body.md:1-4` is explicitly a drafted PR body artifact.                                   | Remove these from the upstream-presented branch or move them to an external handoff repo.                       |
| `studies/swim-37` is a cohort/rebase-study scaffold, not a continuation runtime feature, but it is wired into normal repo gates. | `studies/swim-37/harness/README.md:3-6` pins `canonical2` and `cael/swim-37-trap-classes`; `test/vitest/vitest.config.ts:52-53` adds the swim and durability projects to root Vitest; `tsconfig.json:32` includes `studies/**/*` in root TypeScript. | Keep the harness opt-in or out-of-tree until it is product-generic, upstream-owned test infrastructure.         |
| Workflow manual dispatch became a no-op.                                                                                         | `.github/workflows/workflow-sanity.yml:3-8` advertises `workflow_dispatch`, while jobs are gated off with `if: github.event_name != 'workflow_dispatch'` at `.github/workflows/workflow-sanity.yml:20-85`.                                           | Restore a useful manual path or remove `workflow_dispatch`.                                                     |
| `/status` continuation totals can undercount.                                                                                    | `src/commands/status.command-report-data.ts:145-183` labels aggregate pending/staged counts but iterates only `params.summary.sessions.recent`.                                                                                                      | Source from the full continuation/session index, or label the banner as recent-session-only.                    |
| Gateway protocol exposes internal runner knobs.                                                                                  | `src/gateway/protocol/schema/agent.ts:159-180` accepts `cleanupBundleMcpOnRunEnd` and `drainsContinuationDelegateQueue`; generated Swift models include both fields in `apps/*/OpenClawProtocol/GatewayModels.swift`.                                | Keep internal routing knobs behind internal request envelopes unless third-party clients are meant to set them. |

## Upstream-leakable

| Finding                                                                               | Evidence                                                                                                                                                                                                                          | Recommended fix                                                                                             |
| ------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- |
| RFC author/evidence links point at karmaterminal infrastructure.                      | `docs/design/continue-work-signal-v2.md:4` names `[karmaterminal](https://github.com/karmaterminal)` as author; `docs/design/continue-work-signal-v2.md:1512` links `karmaterminal/karmaterminal-openclaw-docs` Swim 41 evidence. | Replace with upstream-generic authorship/evidence wording or remove fork evidence links before upstream PR. |
| Production code comment leaks internal chronology and trackers.                       | `src/auto-reply/continuation/targeting.ts:114-121` says "Per figs 2026-05-04" and cites `karmaterminal/openclaw#578 / #580`.                                                                                                      | Keep the durable-queue rationale, but remove names, dates, and fork issue refs.                             |
| Workspace `TOOLS.md` template banks fork-only doctrine.                               | `docs/reference/templates/TOOLS.md:49-89` references `karmaterminal-2026.4.24-base`, `karmaterminal/binary-canticle#11`, `prince:<role>...`, `frond-scribe/...`, and candidate hashes.                                            | Delete this section or rewrite as generic plugin/tool descriptor guidance with no cohort/fork refs.         |
| Planning doc contains cohort shorthand, local branches, and Discord-like message ids. | `docs/plan/surfaces-1-2-substrate-notes.md:1-15`, `docs/plan/surfaces-1-2-substrate-notes.md:130-240`.                                                                                                                            | Remove from upstream branch.                                                                                |
| PR-body artifact carries split-history and fork tracker refs.                         | `docs/pr-bodies/pr-38780-body.md:1-4`, `docs/pr-bodies/pr-38780-body.md:36-38`.                                                                                                                                                   | Remove from upstream branch; use GitHub PR body, not tracked repo file.                                     |
| Study harness names cohort/lane internals.                                            | `studies/swim-37/harness/README.md:3-6`; `studies/swim-37/harness/swim-runner.test.ts:1-20`.                                                                                                                                      | Generalize or keep out of upstream default gates.                                                           |

## Surprises

| Finding                                                                                | Evidence                                                                                                                                                                                                                                                                  | Recommendation                                                                                                                                                                   |
| -------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| Changelog does not clearly carry all Finding 1/2/3 entries.                            | `CHANGELOG.md:58-59` has entries for delegate-return classification and actual runtime `continue_delegate(targetSessionKey=...)` routing. I did not find a separate explicit Finding 2 / #581 durable-queue premature-ack entry.                                          | Add a single-line Fixes entry for preserving durable `session-delivery-queue` files for non-attached targeted recipients, with attribution consistent with repo changelog rules. |
| `delegate-return` helper contract is ambiguous.                                        | `src/infra/heartbeat-reason.ts:39-68` maps `delegate-return` to kind `wake`, but `isHeartbeatActionWakeReason()` still returns true only for `manual`, `exec-event`, and `hook`; `src/infra/heartbeat-reason.test.ts:17-23` only covers event-driven wake classification. | Add a direct test and either include `delegate-return` in `isHeartbeatActionWakeReason()` or document why action-wake intentionally excludes continuation wakes.                 |
| Known Path A/Path B dual-consume debt remains.                                         | #589 correctly documents that Path A and Path B still duplicate delegate consumption/spawn shaping. The branch fixes Path A, but the architectural trap remains.                                                                                                          | Do not block Phase 5 solely on this if #589 remains accepted debt, but do not present the branch as having converged the consume paths.                                          |
| The branch includes dist-topology / bundle-entry work beyond the continuation surface. | `tsdown.config.ts:194-214` promotes `auto-reply/reply/agent-runner.runtime` and `auto-reply/continuation/lazy.runtime` entries and includes a fork issue comment at `tsdown.config.ts:208-214`.                                                                           | Keep only if required for real runtime correctness, and scrub fork refs/comments. Otherwise split into a separate upstream build-topology PR.                                    |

## Anchor verifications

| Anchor                                                           | Status                                     | Evidence                                                                                                                                                                                                                                                                                                                                                                                                   |
| ---------------------------------------------------------------- | ------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `src/auto-reply/reply/agent-runner.ts` Path A `doToolSpawn`      | **GREEN**                                  | `src/auto-reply/reply/agent-runner.ts:2682-2710` threads `targetSessionKey`, `targetSessionKeys`, `fanoutMode`, and `traceparent` into `spawnSubagentDirect()` as `continuationTargetSessionKey`, `continuationTargetSessionKeys`, `continuationFanoutMode`, and `traceparent`.                                                                                                                            |
| `src/auto-reply/continuation/targeting.ts` premature ack removal | **GREEN code / RED comment hygiene**       | `src/auto-reply/continuation/targeting.ts:88-122` enqueues durable delivery and in-memory system event, requests `delegate-return` heartbeats, and does not call `ackSessionDelivery()` in the loop. The rationale comment at `src/auto-reply/continuation/targeting.ts:114-121` must be scrubbed for upstream.                                                                                            |
| `src/infra/heartbeat-reason.ts` delegate-return classifier       | **GREEN with helper caveat**               | `src/infra/heartbeat-reason.ts:39-46` classifies `delegate-return` as `wake`; `src/infra/heartbeat-reason.test.ts:17-23` covers event-driven wake classification. Helper ambiguity tracked above.                                                                                                                                                                                                          |
| `docs/design/continue-work-signal-v2.md` targeted semantics      | **GREEN semantics / RED upstream hygiene** | `docs/design/continue-work-signal-v2.md:127-143` defines the terms; `docs/design/continue-work-signal-v2.md:184-208` states that target fields route completion envelopes, not task bodies, and describes next-turn drain plus `silent-wake` `delegate-return` behavior. The same RFC leaks fork evidence at `docs/design/continue-work-signal-v2.md:4` and `docs/design/continue-work-signal-v2.md:1512`. |
| `CHANGELOG.md` Findings 1/2/3                                    | **RED partial**                            | `CHANGELOG.md:58-59` covers Finding 1 and Finding 3 shapes. I did not find a separate explicit Finding 2 / #581 durable-queue premature-ack entry.                                                                                                                                                                                                                                                         |
| `TOOLS.md` 10-line canon bank                                    | **RED / not found**                        | The only changed `TOOLS.md` surface is `docs/reference/templates/TOOLS.md`; it contains a substrate-adoption convention at `docs/reference/templates/TOOLS.md:49-89`, not the claimed 10-line cohort-recognition-engine canon, and the section is upstream-leakable.                                                                                                                                       |

## Recommendation for Phase 4 cohort sign-off

Do not sign off for Phase 5 yet. First make #592 blocking for upstream-readiness and either fix or explicitly accept #593. After those changes, re-run the same review checklist on the recomposed branch, with special attention to:

1. no `karmaterminal`, cohort, branch-lane, local host, Discord-channel, or prince-name artifacts in tracked upstream files;
2. no branch-specific `studies/**` scaffold in default Vitest or root TypeScript unless upstream maintainers explicitly want it;
3. the #581 durable queue fix represented in changelog and comments without fork-local provenance;
4. a clear stance on public Gateway protocol fields vs internal runner controls.
