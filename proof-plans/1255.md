# Proof corpus plan — #1255 abandonment retry budget

Publish under:

`karmaterminal-openclaw-docs:main:PR-NNNNNN/PROOFS/<FULL_SHA>/`

Follow existing `openclaw.proofs.index.v1` / `proofs-manifest.json` rows. Non-continuation. No SWIM.

## Identity freeze

- product SHA (treatment head)
- base: `6b09b1dbe938ab6b5f56eaf4e58f1ed243f89955` or its successor that still contains the bypass
- absorbed upstream under that composite
- fossil file: `src/channels/message/ingress-drain.abandonment-retry-budget.test.ts`
- runner: `node scripts/run-vitest.mjs run --config <owning-shard> --maxWorkers=1 <file>`
- never raw Vitest

## Rows

| Row                        | Title                                                          | State meaning                                          |
| -------------------------- | -------------------------------------------------------------- | ------------------------------------------------------ |
| `W87-1255-RED`             | Eight pre-adoption abandonments terminalize at attempt ceiling | Must be **fail** on unpatched `6b09` / fossil-only SHA |
| `W87-1255-CTRL-BELOW`      | Below-ceiling abandonment stays pending                        | pass before and after                                  |
| `W87-1255-CTRL-CANCEL`     | Cancellation at ceiling is budget-free                         | pass before and after                                  |
| `W87-1255-CTRL-OTHER-LANE` | Unrelated lane completes during poison head                    | pass before and after                                  |
| `W87-1255-CTRL-ONFAILED`   | Ordinary throw still dead-letters with distinct message        | pass before and after                                  |
| `W87-1255-GREEN`           | Unchanged fossil after budget patch                            | pass on treatment SHA                                  |
| `W87-1255-REVERT-RED`      | Patch-only revert restores RED                                 | fail                                                   |
| `W87-1255-REAPPLY-GREEN`   | Reapply restores GREEN                                         | pass                                                   |
| `W87-1255-BASELINE-CLASS`  | Exact-upstream / `6b09` classification of any suite reds       | recorded, not “fixed” unless this issue owns them      |
| `W87-1255-LIVE`            | Isolated Gateway, no Discord send (optional rank-up)           | missing until operator-approved                        |

## Required artifacts

- `INDEX.json`, `proofs-manifest.json`, `METHOD.md`, `README.md`, `RESOLVED-SHA.md`, `SHA256SUMS`
- `artifacts/candidate-manifest.txt`
- `artifacts/red.log` / `green.log` / `revert-red.log` / `reapply-green.log` (sanctioned runner)
- `artifacts/platform.json`
- `artifacts/numstat.txt` (production vs test LOC)
- graph refresh: `causal-proof` intervention nodes + `CAUSES_TEST_PASS` / `REVERT_RESTORES_RED` only after closure

## Negative controls

Do not accept GREEN if any control flips. Do not collapse `turn-abandoned` into `dispatch exploded`. Do not dead-letter on `onCancelled`. Do not change #121204 freshness predicates in this PR.

## Publish / fold

- Publish a **new** `<FULL_SHA>` directory per product/harness/intervention identity. Never append to an old SHA.
- Fold into parent #1254 only as a link + disposition (`apply` / `wait` / `do-not-deploy`).
- Do not fold into `PR-121204/PROOFS/`.
- Do not fold into continuation assembly proofs.
- `Done` on Project 87 only after publication + explicit deploy disposition.
