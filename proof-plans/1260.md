# Proof corpus plan — #1260 reply-turn admission reason

`karmaterminal-openclaw-docs:main:PR-NNNNNN/PROOFS/<FULL_SHA>/`

Non-continuation. No SWIM. Independent of `PR-121204/PROOFS/` and of #1255 abandonment fossils.

## Identity freeze

- product SHA (instrumentation head)
- base composite `6b09b1dbe938ab6b5f56eaf4e58f1ed243f89955` or successor that still collapses the three reasons
- owner: `src/auto-reply/reply/reply-turn-admission.ts`
- runner: `node scripts/run-vitest.mjs run --config <owning-shard> --maxWorkers=1 <file>`
- temp session/store only; no live Silas rows

## Rows

| Row                      | Title                                                                    | Notes                                                        |
| ------------------------ | ------------------------------------------------------------------------ | ------------------------------------------------------------ |
| `R-NC-ADMISSION-REASONS` | Three real `admitReplyTurn` fixtures emit distinct typed reasons         | RED on unpatched `6b09` because reasons are absent/identical |
| `R-NC-ADMISSION-MESSAGE` | User-visible `Session ... changed while starting work. Retry.` unchanged | pass before and after instrumentation                        |
| `R-NC-ADMISSION-SUCCESS` | Successful admission and active-run deferral unchanged                   | pass before and after                                        |
| `W87-1260-GREEN`         | Instrumentation patch                                                    | pass                                                         |
| `W87-1260-REVERT-RED`    | Patch-only revert                                                        | fail (`R-NC-ADMISSION-REASONS`)                              |
| `W87-1260-REAPPLY-GREEN` | Reapply                                                                  | pass                                                         |
| `R-NC-SILAS-REASON`      | Isolated/session-copy receipt names Silas’s branch                       | pending; required before any behavioral successor            |
| `W87-1260-CTRL-118879`   | If reason is `recovery-owner-invalidated`, record #118879 applicability  | do not duplicate that PR here                                |

## Required artifacts

- `INDEX.json`, `proofs-manifest.json`, `METHOD.md`, `README.md`, `RESOLVED-SHA.md`, `SHA256SUMS`
- `artifacts/candidate-manifest.txt`
- `artifacts/red.log` / `green.log` / `revert-red.log` / `reapply-green.log`
- `artifacts/reason-matrix.json` (three fixtures → reason codes; no session secrets)
- `artifacts/message-unchanged.diff` or equivalent assertion log
- `artifacts/platform.json`

## Negative controls

Do not accept GREEN if visible text changes. Do not dead-letter or alter retry budget. Do not claim Silas cured. Do not fold #1255 eight-abandon fossil into this corpus. Do not guess `recovery-owner-invalidated` without `R-NC-SILAS-REASON`.

## Publish / fold

New `<FULL_SHA>` directory per product/harness/intervention identity. Fold into #1254 as “Silas producer now classifiable,” never “Silas loop cured.” That cure remains #1255 (budget) plus a later owner-selected successor if needed.
