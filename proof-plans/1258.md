# Proof corpus plan — #1258 ingress terminal-outcome lineage

`karmaterminal-openclaw-docs:main:PR-NNNNNN/PROOFS/<FULL_SHA>/`

## Identity freeze

- product SHA
- schema version **unchanged** (state v7 on `6b09`; no bump)
- uses existing `completed_metadata_json`
- isolated channel / nonce (live row requires operator approval)

## Rows

| Row                        | Title                                                                                       | Notes                               |
| -------------------------- | ------------------------------------------------------------------------------------------- | ----------------------------------- |
| `W87-1258-ENUM`            | Every terminal enum round-trips                                                             | unit                                |
| `W87-1258-REDACT`          | Completed row has no message payload                                                        | unit                                |
| `W87-1258-GATE-VS-ADOPT`   | Policy-gate vs adopted-agent-run distinguishable                                            | unit                                |
| `W87-1258-MONITOR-DEFAULT` | `ingress-monitor.ts` no-dispatch path writes a gate/silence reason, not a fake run          | unit                                |
| `W87-1258-121204-VALUE`    | Stale-ambient completion, if present, is its own enum                                       | negative: do not invent Rune owner  |
| `W87-1258-NONCE`           | 1 admitted event → 1 run or explicit gate → 1 terminal → snowflake or recorded non-delivery | isolated live/harness               |
| `W87-1258-RUNE-CLASSIFY`   | After deploy, one Rune-class probe classified                                               | **observability**, not a speech fix |
| `W87-1258-INTERVENTION-*`  | GREEN / revert / reapply on the metadata writer                                             | required for the code PR            |

## Required artifacts

- standard INDEX/METHOD/README/RESOLVED-SHA/SHA256SUMS/manifest
- `artifacts/enum-matrix.json`
- `artifacts/completed-row-redacted.json`
- `artifacts/nonce-lineage.json` (event id, run id or gate, terminal enum, outbound id or `none`)
- `artifacts/schema-version.txt` proving no bump
- no message bodies, no user identifiers

## Negative controls

No second column. No dual-write. No identity inference from session keys. No Rune delivery patch in this PR. No live probe on the production room without isolated-channel approval.

## Publish / fold

New SHA directory. Fold into #1254 as “Rune now classifiable,” never “Rune cured.” Do not fold into #121204 proofs except a cross-link that stale-ambient is one enum value.
