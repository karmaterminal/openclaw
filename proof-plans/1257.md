# Proof corpus plan — #1257 synchronous transcript SQLite holds

`karmaterminal-openclaw-docs:main:PR-NNNNNN/PROOFS/<FULL_SHA>/`

## Identity freeze

- product SHA
- **immutable** copied agent DB + WAL/SHM (Gateway stopped or snapshot-verified)
- never the live seat files
- query text of the exact fenced read

## Rows

| Row                       | Title                                                        | Notes                                              |
| ------------------------- | ------------------------------------------------------------ | -------------------------------------------------- |
| `W87-1257-COPY-INTEGRITY` | `quick_check` / `integrity_check` on copy                    | record; corruption → separate lane                 |
| `W87-1257-STAT1-BEFORE`   | `sqlite_stat1` presence + sample                             | candidate input for #119901                        |
| `W87-1257-PLAN-BEFORE`    | `EXPLAIN QUERY PLAN` + timed `loadTranscriptEventsSync`      | baseline                                           |
| `W87-1257-ANALYZE-COPY`   | Same measurements on a **second** copy after `ANALYZE`       | counterfactual; not production                     |
| `W87-1257-SEMANTICS`      | Transcript seq/bytes equal before/after any read-path change | pass required                                      |
| `W87-1257-SMALL-SESSION`  | Tiny session control does not regress                        | pass                                               |
| `W87-1257-INTERVENTION-*` | GREEN / revert RED / reapply if a product patch ships        | only if not “docs-only ANALYZE recipe”             |
| `W87-1257-119901`         | Applicability of upstream PR #119901                         | `applicable` / `not-applicable` with copy evidence |
| `W87-1257-NOT-INGRESS`    | Observed path is agent DB, not state-DB ingress              | recorded                                           |

## Required artifacts

- INDEX/METHOD/README/RESOLVED-SHA/SHA256SUMS/manifest
- `artifacts/copy-hash.txt`
- `artifacts/pragma.json` (integrity, page_count, freelist_count, stat1)
- `artifacts/explain-before.txt` / `explain-after.txt`
- `artifacts/timing.json` (wall/CPU, n runs)
- `artifacts/transcript-eq.json`
- no live `VACUUM` logs (that would be a mutation incident)

## Negative controls

No live ANALYZE/VACUUM/checkpoint. No claiming #119901 without `STAT1-BEFORE` + timing delta. No combining with #1256 owner repair. File size is not a freelist proxy.

## Publish / fold

New SHA per copy identity and per intervention. If only a doctor recipe is proved, say `offline-maintenance`, not `hot-path-cured`. Do not fold into #121204.
