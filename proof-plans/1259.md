# Proof corpus plan — #1259 prepared route materialization reason

`karmaterminal-openclaw-docs:main:PR-NNNNNN/PROOFS/<FULL_SHA>/`

## Identity freeze

- product SHA
- isolated session, delivery disabled
- no Elliott live config writes
- no credentials in artifacts

## Rows

| Row                        | Title                                                   | Notes                                               |
| -------------------------- | ------------------------------------------------------- | --------------------------------------------------- |
| `W87-1259-PRED-MISSING`    | `missing-model`                                         | RED before reason patch if current throw is generic |
| `W87-1259-PRED-PROVIDER`   | `provider-mismatch`                                     | same                                                |
| `W87-1259-PRED-MODEL`      | `model-id-mismatch`                                     | same                                                |
| `W87-1259-PRED-ROUTE`      | `route-mismatch`                                        | same                                                |
| `W87-1259-CTRL-EXACT`      | Exact prepared route still materializes                 | pass before and after                               |
| `W87-1259-REDACT`          | Logs/fallback rows contain class only, no secrets       | pass                                                |
| `W87-1259-FALLBACK-RECORD` | Existing fallback decision carries the typed reason     | pass after reason patch                             |
| `W87-1259-INTERVENTION-*`  | GREEN / revert / reapply                                | reason-only PR                                      |
| `W87-1259-OWNER`           | Isolated one-candidate run naming the Elliott predicate | after reason patch; delivery disabled               |
| `W87-1259-NOT-401-OWNER`   | Visible Copilot 401 is a control, not this fossil       | do not “fix” by dropping fallback here              |
| `W87-1259-NOT-CLI-PROBE`   | `models status --probe` is not this chain               | recorded                                            |

## Required artifacts

- standard INDEX/METHOD/README/RESOLVED-SHA/SHA256SUMS/manifest
- `artifacts/reason-matrix.json`
- `artifacts/fallback-decision-redacted.json`
- `artifacts/focused-tests.log`
- `artifacts/secret-scan.txt` (no tokens/urls)
- optional `artifacts/owner-probe.json` after operator-approved isolated run

## Negative controls

No fallback reorder. No reauth. No weakening `modelMatchesPreparedTarget`. No #114603 repair unless the typed reason is exactly that mismatch. No continuation.

## Publish / fold

New SHA per reason-only and per later owner-fix. Do not fold Copilot cleanup into this corpus.
